# CoinSvr FundingHedger 자동 튜닝 — 세션 간 연속성 로그

이 파일은 `CLAUDE_CODE_TASK.md`에 정의된 12시간 자동 튜닝 사이클이 세션 간 맥락을 이어가기 위해
누적 기록하는 파일이다. 매 사이클 종료 시 하단에 새 항목을 append하며, 기존 내용은 지우지 않는다.

관련 파일:
- 수치 변경 이력: `CoinSvr/bin/Debug/net9.0/strategy_config_history.jsonl`
- 사이클별 요약: `automation_summary.log` (프로젝트 루트, `CoinSvr/bin/Debug/net9.0/`가 아님 —
  Claude Code 자신이 만드는 메타파일이라 .bat이 cd한 위치에 생성됨. trade_results.jsonl/
  strategy_config.json 등 C# 앱이 쓰는 파일과는 경로가 다르니 혼동 주의)
- 실거래 결과: `CoinSvr/bin/Debug/net9.0/trade_results.jsonl`
- 스킵 시뮬레이션: `CoinSvr/bin/Debug/net9.0/skipped_results.jsonl`
- 날짜별 아카이브(2026-07-19 신설): `CoinSvr/bin/Debug/net9.0/DataArchive/<YYYYMMDD>/<파일명>` —
  `rotate_data_logs.ps1`이 "오늘 이전" 날짜 레코드를 여기로 옮긴다(라이브 파일 무한증가 방지).
- 증분분석 상태파일(2026-07-19 신설): `CoinSvr/bin/Debug/net9.0/analysis_state.json` —
  `analyze_cycle.ps1`이 "마지막 분석 이후" 컷오프(`LastCutoffUtc`)를 자동 관리. 사람/이 작업 모두
  직접 편집하지 않는다.

⚠️ **2026-07-19 데이터 유실 사고**: 사람과의 대화 세션에서 `rotate_data_logs.ps1`(위 로테이션
인프라)을 만들던 중 `-WhatIf`(미리보기) 모드 구현 버그로 `trade_results.jsonl`(2026-07-10~07-18,
112줄)과 `skipped_results.jsonl`(2026-07-14~07-18, 41줄) 및 spike_scalp 계열 파일들의 **2026-07-19
이전 전체 이력이 삭제됨**(git 저장소 아님, 휴지통/VSS 백업 전무로 복구 실패 확인, `.ui` 로그로
부분 재구성 가능했으나 사람이 진행하지 않기로 결정). 버그 자체는 수정 완료(WhatIf 모드가 이제
라이브 파일을 실제로 건드리지 않음, 격리 테스트로 검증됨). **이 파일의 5~33회차 요약 서술은 그대로
유효한 기록이지만, 그 근거였던 원본 raw 레코드는 더 이상 존재하지 않으므로 재검증이 불가능하다**는
점을 인지할 것 — 2026-07-19 이후 신규 데이터부터는 로테이션+아카이브로 안전하게 보존된다.

## 과거 요약 (5~37회차 + 사람과의 대화 세션 2건, 2026-07-11T06:41:14Z ~ 2026-07-21T10:33:28+09:00)
CLAUDE.md가 20KB 압축 임계값을 다시 넘어 이 구간을 압축함. 5~34회차 상세는 기존 아카이브
(`CLAUDE_MD_ARCHIVE_20260713.md`~`CLAUDE_MD_ARCHIVE_20260720.md`), 35~37회차 상세는
`CLAUDE_MD_ARCHIVE_20260720B.md`, 07-20/07-21 사람과의 대화 세션 전문은
`CLAUDE_MD_ARCHIVE_20260721.md`에 보존.

5~24회차: 파라미터 변경 없음 지속. 트레일링 giveback 35%초과 반복(근본원인 규명+수정, 아래 참고).
**2026-07-15 코드패치 3건**: B_ProfitPct_Est 펀딩비 누락버그 수정, tick/price 비율 후보필터
(`MaxTickPriceRatioPct` 0.1%) 도입, 펀딩비크기 vs 반락폭 상관계수0.43(n=76) 확인. **2026-07-16**:
SnapA/B는 실거래 판단에 안 쓰이고 사후분석용뿐 - 실거래 추세필터는 `Trend30Pct` 사용,
SnapSameTick=true 레코드는 A_Est 비교 제외.

25~37회차: "A_Est가 C상회" 누적이 25회차 이래 계속 뒤집히다 35회차 11:11 동률 → 36회차 11:14
C우위 → 07-20 사람세션 11:16. "추세약함→B유리/추세강함→C유리"는 25~35회차 11연속 지지 후
36회차(양끝구간 n=1씩)에서 첫 반박. 펀딩비크기 3구간은 33~35회차 "중간구간 최고치" 비단조
패턴 재현 후 36회차 이탈(고구간최저, ESPORTSUSDT 2.0%는 이상치 가능성). 양수펀딩 표본 누적
3건(EBAYUSDT+FWDIUSDT, 2종)에서 장기 정체. `analyze_cycle.ps1` 구간별 승률표시 버그 미해결
(known bug, 매 사이클 무시하고 평균값만 신뢰). Collection was modified 스캔예외 누적 5건
(30~37회차) - 07-20 사람세션에서 근본원인 규명+수정(아래 참고), 배포 이후 신규 0건으로 효과
확인됨. 37회차 09:30 KST 재시작은 SpikeScalp쪽 세션의 공유 프로세스 재시작이 원인(FundingHedger
자체와 무관, 확인 완료). `DataArchive` 폴더 07-20 UTC 전환 시점 정상 최초생성 확인.

**07-20 사람과의 대화 세션 (코드버그 2건 근본원인 규명+수정)**: (1) `Fundinghedgemanager.cs`
`DispatchAsync` 미끼선정 루프에서 순회 중인 `baitCandidates`를 안쪽 foreach가 직접 `Remove`하던
버그 발견 - `.ToList()` 스냅샷 순회로 수정(Collection was modified 예외 5건의 근본원인, 파일명은
`Fundinghedger .cs`가 아니라 `Fundinghedgemanager.cs`). (2) 트레일링청산 giveback이 35%를
45~55%까지 초과하던 근본원인을 BOTUSDT/ACEUSDT 로그 초단위 대조로 규명 - 정산직후 급락구간에서
`TRAIL_GIVEBACK_CONFIRM_TICKS`(2틱 디바운스)가 확인용 1틱만큼 반납을 더 진행시킨 뒤 청산하는
구조적 지연이 원인. `TRAIL_GIVEBACK_HARD_MULT`(1.3배 초과시 디바운스 생략 즉시청산) 코드 추가 +
`TrailGivebackPct` 35→28 하향. 이어서 후속 조사로 50ms 폴링보다 `Fundinghedgemanager.cs`
BookTicker 콜백의 100ms 스로틀(`BOOK_TICKER_MIN_INTERVAL_MS`)이 실질 병목이었음을 규명해,
트레일링 감시 심볼만 스로틀 우회하는 이벤트기반 구조(`RegisterExitMonitor`/`WaitForNextBookTickerTickAsync`)
로 전면 교체. **2026-07-20T13:00:16Z 빌드+재시작 배포 완료**(PID 38392→49000, 정상 확인).
DailyLossLimitUsdt 20→30은 07-11 최초부터의 값으로 당일과 무관함을 확인해 해소. 스코프 밖 이슈
1건(skipped_results.jsonl 파일락 경합 예외, 미수정)만 별도 기록.

**07-21 사람과의 대화 세션 (PC재부팅 데이터공백 + 자동시작 등록)**: 07-20 23:47 KST 재부팅 후
`CoinSvr.exe` 자동시작 스케줄이 없어 사람이 수동 재기동한 07-21 10:33까지 약10시간46분 서비스
정지(데이터공백)됐던 것을 규명, 신규 스케줄 작업 `CoinSvr_AutoStart`(로그온 시 트리거, IgnoreNew)
등록 완료 - **아직 실제 재부팅으로 트리거 자체는 미검증** 상태(이 작업 범위 밖, 계속 관찰만).

## 자동튜닝 사이클 로그 [2026-07-21T15:39:28+09:00] (38회차)
- 표본: 9건 (trade 5 + skip 4, 기간: 2026-07-20T10:23:44Z ~ 2026-07-21T06:36:32Z, 자동컷오프
  약20시간 - 이 구간에 사람과의 대화세션 2건, 07-20T13:00Z 코드배포, 07-20 23:47 KST PC재부팅이
  모두 걸쳐 있음)
- A/B/C 비교 (trade 5건, paper): C=평균-0.0751%/승률40%(2/5), A_Est=평균0.0253%/승률60%(3/5),
  B_Est=평균-0.0571%/승률80%(4/5). non-bait 2건만: C=평균-0.1061%/승률50%. 실측(Actual) 5건
  전부 null이라 실측 비교는 여전히 불가.
- 발견한 패턴: (1) "A_Est가 C상회" 개별비교 이번 A4승C1승 → 누적 11:16(C리드5칸)에서 15:17(C리드
  2칸)로 급격히 좁혀짐 - "구조적 우열 없음" 가설 방향에 부합, 단일사이클 스윙폭이 큼(지속 확인 필요).
  (2) Trend30Pct 강한추세 구간(n=3)에서 B_Est(0.1685%)가 C(-0.0836%)를 크게 앞서 "추세강함→C유리"
  가설이 36회차(n=1)에 이어 n=3 표본으로 재차 반박됨 - 25~35회차 11연속 지지에서 최근 2연속 반례로
  전환, 가설 신뢰도 재검토 필요. (3) 펀딩비크기 3구간 "중간구간최고치" 비단조 패턴이 36회차 이탈 후
  다시 재현(저-0.1392%/중+0.0724%/고-0.1816%, 각n=3). (4) 트레일링청산 유효표본(DODOXUSDT45.5%,
  DEXEUSDT33.3%) - 07-20T13:00Z TrailGivebackPct=28+하드컷+이벤트기반 배포 이후 giveback이 목표28%
  대비 여전히 초과하나, 배포 전 BOTUSDT54.5%/ACEUSDT45.0%보다는 개선 방향(n=2뿐이라 확정 아님).
  (5) ERAUSDT giveback1033.3%는 peak0.0307%가 TrailMinPeakPct(0.05%) 미달로 트레일링 자체가
  작동 안 한 상태에서 발생한 별종 극단치 - 기존 THEUSDT/1000XECUSDT급 저유동성 급변과 동일 계열
  추정, giveback 로직 문제 아님. (6) Collection was modified 스캔예외: 07-20T13:00Z 배포(foreach
  스냅샷 수정) 이후 07-20 잔여시간+07-21 전체 .ui 로그에서 신규 0건 - 버그수정 효과 확인됨(누적
  5건에서 정지). (7) 구간별 승률표시는 기존 known bug로 이번에도 전구간 0%로 나와 신뢰 불가,
  평균값만 사용.
- 파라미터 변경: 없음 - trade 5건으로 최소기준은 충족하나 세부 비교군(트레일링청산 n=2, non-bait
  n=2 등)이 전부 5건 미만이라 방향성 확신 부족(규칙5 적용, 특히 TrailGivebackPct는 배포 직후라
  효과 검증 표본 자체가 더 필요).
- 롤백: 해당없음 (config history에 자동튜닝 사이클발 변경이 아직 0건 - 07-20 사람세션발 1건뿐이라
  "직전3 vs 이전3" 비교 자체 불가).
- 다음 사이클이 알아야 할 것: (1) TrailGivebackPct=28 배포 이후 giveback 수렴 여부를 표본 5건
  이상 쌓일 때까지 계속 추적할 것(이번 n=2는 판단 근거로 부족). (2) "추세강함→C유리" 가설이 최근
  2연속(36회차 n=1, 38회차 n=3) 반박됐으므로, 다음 사이클에 강한추세 구간 표본이 더 쌓이면 이
  가설 자체를 "기각 검토" 단계로 격상할지 판단할 것. (3) "A_Est가 C상회" 누적이 15:17로 좁혀진
  추세가 다음 사이클에도 이어지는지, 아니면 이번이 일시적 스윙이었는지 확인. (4) 07-21 PC재부팅
  이후 등록된 `CoinSvr_AutoStart` 스케줄 작업은 아직 실제 재부팅으로 검증되지 않은 상태 그대로임 -
  이 사이클 범위 밖이라 별도 조치 안 함.

## 자동튜닝 사이클 로그 [2026-07-21T21:36:32+09:00] (39회차)
- 표본: 8건 (trade만, skip 신규 0건, 기간: 2026-07-21T06:36:38Z ~ 2026-07-21T12:36:32Z, 약6시간 -
  오랜만에 정확히 6시간 주기 컷오프, 사람 대화세션 개입 없음)
- A/B/C 비교 (전체8건, paper): C=평균0.0354%/승률87.5%(7/8), A_Est=평균0.1367%/승률75%(6/8),
  B_Est=평균-0.0361%/승률50%(4/8). non-bait(실제C진입) 5건만: C=평균-0.0744%/승률80%(4/5) -
  ERAUSDT 1건(MaxHoldMs타임아웃, C=-0.7365%)이 평균을 크게 끌어내림, 이 건 제외시 나머지4건은
  전부 플러스. 실측(Actual)은 여전히 전부 null.
- 발견한 패턴: (1) "A_Est가 C상회" 개별비교 이번 A4승C4무(동률) → 누적 A15:C17에서 A19:C21로,
  C우위 격차(2칸)는 그대로 유지된 채 정체 - 38회차의 급격한 좁혀짐 추세가 이번엔 멈춤.
  (2) ⭐추세강도 이중가설("약함→B유리/강함→C유리")이 이번 사이클에 강하게 재지지됨 - 약추세
  (|Trend30|<0.355, n=3, 표본부족)에서 C=-0.1445%/B_Est=+0.2595%로 B우위, 강추세대조(|Trend30|
  >0.2, n=5, 규칙상 유효표본)에서 C=0.1433%/B_Est=-0.2135%로 C우위 - 36·38회차의 2연속 반례를
  방향상 뒤집는 결과(다만 이 가설은 사이클마다 스윙이 커서 39회차 단독으로 "재확정"은 이름,
  최소 1~2사이클 더 관찰 필요). (3) 트레일링청산 giveback 첫 5건 이상 유효표본(n=7, 07-20T13:00Z
  하드컷+이벤트기반+파라미터하향 배포 이후 최초): 평균31.4%(TREEUSDT-33.3%/ERAUSDT60.0%/
  LAUSDT57.1%/DEXEUSDT33.3%/LAUSDT62.5%/DEXEUSDT40.0%/DODOXUSDT0%) - 목표28%에 근접했으나
  개별건 편차가 매우 큼(-33.3%~62.5%), "근접하되 편차 큼"으로 잠정 결론. (4) peak=0%
  MaxHoldMs타임아웃 1건(ERAUSDT, Trend30=-0.0819 근사0) 추가 - 모멘텀 오탐 진입 가설 사례 누적
  계속. (5) 펀딩비크기 3구간(저n=3/중n=2/고n=3, 전부 표본부족)이 이번엔 저0.218%→중0.037%→
  고-0.149%로 단조감소 - 기존 "중간구간 최고치" 비단조 패턴과도 다른 제3의 모양, 표본부족으로
  결론 보류. (6) 펀딩방향: 이번 8건 전부 음수, 양수펀딩 표본은 여전히 4종 정체(B전략 유리조건
  분석용 |Trend30|<0.05 최극단 구간도 이번 표본 0건). (7) `analyze_cycle.ps1`의 Trend30 강구간
  (>=1.523, n=3) 출력(C=0.1093%/B_Est=0.4016%)이 trade_results 원본 2건 직접계산(C평균0.271%/
  B_Est평균-0.489%)과 어긋남 - skipped_results 병합레코드 1건이 섞여든 것으로 추정되나 미확인,
  기존 "구간별 승률표시 버그"의 또다른 증상일 가능성. 이번 사이클 서술은 원본 2건 직접계산
  기준으로 작성.
- 파라미터 변경: 없음 - 표본이 전체는 8건(최소기준 충족)이나 세부비교군은 거의 전부 5건
  미만(트레일링청산만 n=7로 예외)이라 방향성 확신 부족(규칙5 적용). 트레일링 giveback n=7은
  목표치 근접이 확인됐으나 추가 하향을 정당화할 만큼 초과폭이 크지 않아 유지.
- 롤백: 해당없음 (config history에 자동튜닝 사이클발 변경 누적 0건, "직전3 vs 이전3" 비교 불가)
- 다음 사이클이 알아야 할 것: (1) 트레일링 giveback n=7 평균31.4%가 다음 사이클에도 유지/수렴
  되는지 표본을 더 쌓아 확인. (2) 추세강도 이중가설이 이번에 강하게 재지지됐으나 스윙이 큰
  지표라 최소 1~2사이클 더 지켜본 뒤 "가설 확정" 여부 판단할 것. (3) "A_Est가 C상회" 누적격차
  (2칸)가 이번엔 안 좁혀졌는데 다음 사이클에 다시 좁혀지는 추세로 복귀하는지 확인. (4)
  analyze_cycle.ps1의 강추세구간 출력과 원본레코드 수동계산이 어긋난 건(패턴 7) - 스크립트
  디버깅은 이번 사이클 범위 밖이라 보류, 반복되면 다음 세션에서 스크립트 자체 점검 필요.
  (5) `CoinSvr_AutoStart` 스케줄 작업은 여전히 실제 재부팅으로 미검증 상태.
