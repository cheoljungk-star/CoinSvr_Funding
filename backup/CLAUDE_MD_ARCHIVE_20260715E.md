## 자동튜닝 사이클 로그 [2026-07-15T08:06:00Z] (19회차)
- 표본: 0건 (기간: 18회차 컷오프 2026-07-15T07:03:28Z 이후 ~ 2026-07-15T08:06:00Z UTC, 약 1시간). `analyze_cycle.ps1` 기본 12시간 창으로는 5건이 잡히지만 전부 최신 레코드가 2026-07-15T04:00:15Z로 18회차에서 이미 보고된 것과 동일 트레이드(DATAIPUSDT/VANRYUSDT 등) — 신규는 0건. 18→19회차 2사이클 연속 표본 0건.
- 참고용 A/B/C: 해당없음(표본 0건).
- 🔧 **`analyze_cycle.ps1` 인코딩/버그 수정**: 스크립트가 BOM 없는 UTF-8로 저장돼 있어 Windows PowerShell 5.1이 한글 문자열을 시스템 코드페이지로 오독, 매 실행마다 "Unexpected token" 파싱 에러로 실패하던 것을 확인 — UTF-8 BOM으로 재저장해 해결(로직 변경 없음, 인코딩만 수정). 추가로 `$timedOut`/`$trailed`가 결과 1건일 때 PowerShell이 배열을 스칼라로 언랩해 `.Count`가 `$null`이 되는 버그를 발견해 `@()` 래핑으로 수정. 둘 다 `.ps1` 보조 스크립트 수정이라 "C# 소스코드(.cs) 수정 금지" 규칙과 무관 — 참고로 남김.
- 🚨 **신규 인프라 이상 2건 발견(`.ui` 로그, 이번 구간 04:00~08:06 UTC)**:
  1. 2026-07-15 05:00:15 KST: VANRYUSDT PreTrendSkip 기록 중 `skipped_results.jsonl`에 대한 IOException("being used by another process") — 해당 스킵 레코드 유실(직후 GWEIUSDT 기록은 정상). 오늘 1회만 관측된 간헐적 파일 잠금 경합.
  2. 2026-07-15 16:59:58 KST(07:59:58 UTC): FWDIUSDT 미끼 진입이 "Please sign TradFi-Perps agreement contract fapi"로 거부된 직후 `❌ 스캔 사이클 예외: Collection was modified; enumeration operation may not execute` 발생 — 해당 스캔 사이클만 중단되고 다음 정기 스캔은 정상 재개. TradFi-Perps 거부 자체(SKHYNIXUSDT/SAMSUNGUSDT/FWDIUSDT, 오늘 5회)는 계정에 계약 미서명이라 항상 나는 기존 패턴이나, 동시성 예외는 오늘 처음 관측.
  둘 다 스레드 안전성 관련 `.cs` 버그로 추정 — 이 작업 권한(.cs 수정 금지) 밖이라 **사람 검토가 필요한 사항으로 플래그만 하고 코드는 건드리지 않음**. 재발 빈도가 늘면 다음 사이클에서 더 강하게 재플래그할 것.
- GWEIUSDT tick/price 필터: 2026-07-15T03:00 UTC 이후 배제 상태가 이번 구간(~13시간)에도 계속 유지, 재등장 없음 — 필터 정상 작동 재확인.
- 파라미터 변경: 없음 — 표본 0건, "확신 없으면 안 바꾼다" 원칙 자명 적용.
- 롤백: 해당없음 — 비교 대상 자체가 없음(유효표본≥5건 사이클 여전히 12·14회차 둘뿐).
- 다음 사이클이 알아야 할 것:
  - **"A_Est가 C상회" 3/3(13,14,16회차) 재현 여부 재확인이 여전히 최우선 과제** — 18,19회차 연속 표본 0건이라 계속 이월 중. 다음에 표본 5건 이상 쌓이면 반드시 최우선 확인.
  - `skipped_results.jsonl` 파일 잠금 경합(IOException)과 스캔 사이클 "Collection was modified" 예외 재발 여부를 계속 관찰 — 빈도가 늘면 사람에게 더 적극적으로 에스컬레이션.
  - TradFi-Perps 미서명 거부(SKHYNIXUSDT/SAMSUNGUSDT/FWDIUSDT)가 반복되는데, 이 심볼들을 후보에서 아예 제외할지는 후보선정 로직 변경(이 작업 범위 밖)이라 사람이 판단할 사항 — 참고로만 남김.
  - 이번 사이클 종료 직전 CLAUDE.md가 20.1KB로 임계값을 넘어 17~18회차 상세 로그를 `CLAUDE_MD_ARCHIVE_20260715D.md`로 이동, "과거 요약"을 5~18회차로 확장(20173→11879바이트로 압축). `automation_summary.log`도 함께 21.3KB로 넘어 동일 절차 적용함(아래 참고, 해당 로그 파일에서 확인).
  - (기록 메모) 이번 사이클에서 회차 번호 부여 오류를 발견해 정정함: 07:03:28Z 실행분은 실제로는 18회차인데, 이번(08:06:00Z) 사이클을 처음 작성할 때 실수로 "17회차 컷오프"라 잘못 참조하고 스스로도 "18회차"라 잘못 이름 붙였었음 — CLAUDE.md/automation_summary.log 양쪽에서 19회차로 정정. 다음 사이클은 이번(19회차) 기준으로 번호를 이어갈 것.
  - (기록 메모 정정) 위 정정 작업 중 automation_summary.log 쪽은 실제로는 라벨이 고쳐지지 않고 "18회차"로 남아있었음을 20회차에서 재발견 — 20회차 압축 작업 때 타임스탬프(08:06:00Z) 기준으로 19회차로 바로잡아 반영함. 회차 번호 정정은 "정정했다"고 기록만 남기지 말고 그 파일을 다시 열어 실제로 바뀌었는지 확인할 것.

## 자동튜닝 사이클 로그 [2026-07-15T08:23:14Z] (20회차)
- 표본: 0건 (기간: 19회차 컷오프 2026-07-15T08:06:00Z 이후 ~ 2026-07-15T08:23:14Z UTC, 약 17분 — 사람이 정규 6시간 주기보다 훨씬 짧은 간격으로 수동 실행). trade_results.jsonl/skipped_results.jsonl 둘 다 신규 라인 0건(총 69건/12건 그대로) — analyze_cycle.ps1 기본 12시간 창이 잡아낸 5건은 전부 16·17회차에서 이미 보고된 동일 트레이드.
- 참고용 A/B/C: 해당없음(표본 0건, 3사이클 연속 0건: 18·19·20회차, 17회차까지 포함하면 4사이클 연속 최소기준 미달).
- 인프라 확인: 19회차가 신규 발견한 2건(skipped_results.jsonl IOException 파일잠금, FWDIUSDT 거부 직후 "Collection was modified" 스캔 예외) 모두 이번 17분 구간에 재발 없음(.ui 로그 마지막 라인은 17:11:38 KST ListenKey keepalive 정상). 거래중단/청산실패/수동확인/중복/DailyLossLimit 키워드 0건. GWEIUSDT는 tick/price 필터(0.223%)에 계속 걸려 제외 유지 — 필터 정상 작동 재확인. strategy_config_history.jsonl은 여전히 2026-07-10 초기값 1건뿐(자동튜닝이 실제 파라미터를 바꾼 적이 지금까지 한 번도 없음).
- 파라미터 변경: 없음 — 표본 0건.
- 롤백: 해당없음 — 비교 대상 자체가 없음(유효표본≥5건 사이클 여전히 12·14회차 둘뿐).
- 🔧 automation_summary.log가 20KB 임계값(21267바이트)을 넘어 1~15회차 상세를 `AUTOMATION_SUMMARY_ARCHIVE_20260715B.log`로 압축 이동(원본 보존), 남은 파일은 12347바이트로 축소. CLAUDE.md는 이번 사이클 기준 아직 20KB 미만이라 압축 불필요.
- 다음 사이클이 알아야 할 것:
  - **"A_Est가 C상회" 3/3(13,14,16회차) 재현 여부 재확인이 여전히 최우선 과제** — 17~20회차 4사이클 연속 표본부족/0건이라 계속 이월 중. 다음에 표본 5건 이상 쌓이면 반드시 최우선 확인.
  - 19회차가 발견한 두 인프라 이상(파일 잠금 IOException, 동시성 컬렉션 예외)은 이번 사이클엔 재발 없었으나 관측 구간이 17분으로 매우 짧아 "해소됐다"고 판단하기엔 이름. 다음 사이클에서도 계속 관찰.
  - 이번처럼 직전 사이클 종료 몇 분~수십분 뒤에 다시 실행되는 경우, analyze_cycle.ps1의 12시간 고정 창은 항상 과거 회차와 겹치는 레코드를 다시 보여주므로, "신규 레코드 = 0"인지 **직전 사이클의 진짜 컷오프 시각과 직접 대조**해서 판단해야 함(이번에도 그렇게 확인함) — 이 절차를 스크립트에 반영할지는 다음 기회에 검토.
