# CLAUDE.md 압축 아카이브 (17회차, 2026-07-15)

이 파일은 `CLAUDE.md`가 20KB 압축 임계값을 넘어 17회차(사이클 2회 실행분: 06:43:00Z, 07:03:28Z)
상세 로그를 이동한 것이다. 요약은 `CLAUDE.md`의 "과거 요약" 섹션 참고.

## 자동튜닝 사이클 로그 [2026-07-15T06:43:00Z]
- 표본: 2건 (기간: 16회차 컷오프 2026-07-15T00:39:51Z 이후 ~ 2026-07-15T04:00:16Z UTC) — 최소기준(5건) 미달. 단 이번 사이클 도중 중요한 코드배포 확인 사건이 있어 그 내용 위주로 기록한다. `.ui` 로그에서 거래중단/청산실패/수동확인/중복/DailyLossLimit 키워드 0건. IsBait=false 2건(DATAIPUSDT, VANRYUSDT).
- 참고용 A/B/C(n=2, 결론 근거로 쓰기엔 너무 작음): C=평균+0.328%/승률100%(DATAIPUSDT+0.2475%, VANRYUSDT+0.4081%) vs A_Est=평균+0.041%/승률100% vs B_Est=평균+0.072%/승률100%(둘 다 패치 이후 정상값, 아래 참고) — 2건 다 paper/실측 부호 일치(Actual DATAIPUSDT+0.0135%, VANRYUSDT+0.2032%), 둘 다 peak=종가(giveback0%, MaxHoldMs 타임아웃 중 반락없이 종료).
- 🔧 **코드패치 배포 확인(2026-07-15 세션에서 예고된 3건 중 2건)**: `CoinSvr.dll/.exe` 빌드시각이 2026-07-15 12:11:30 KST(=03:11:30Z)로 확인됨 — `StrategyConfig.cs`에 `MaxTickPriceRatioPct`(기본 0.1%) 필드가 실제로 추가돼 있고 컴파일 정상. `.ui` 로그에서 12:59 KST(03:59Z) 사이클에 최초로 `🔍 [FUNDING-MGR] tick/price 비율 초과로 제외(0.1% 초과): ...GWEIUSDT(0.210%)...` 메시지 확인(GWEIUSDT 포함 21개 심볼 동시 제외 — 배포 직후 첫 스캔이라 누적 후보가 한번에 걸러진 것으로 보임), 13:59·14:59 KST에도 GWEIUSDT(0.214~0.216%) 계속 제외 — **필터가 실제로 작동 중임을 확인**. 그 결과 GWEIUSDT는 2026-07-15T03:00 UTC(마지막 스킵기록) 이후 trade_results/skipped_results 양쪽에서 완전히 사라짐 — 9~16회차에 걸쳐 누적 관측되던 "GWEIUSDT 반복 스킵/전패" 패턴이 이 필터로 원천 차단된 것으로 보임(다음 사이클에 GWEIUSDT 재등장 여부로 재확인할 것). B_Est 신규 2건(DATAIPUSDT+0.098%, VANRYUSDT+0.046%, 둘 다 양수·승률100%)은 패치 이전 누적 관측(거의 항상 열위)과 달라 "B_Est가 버그 때문에 저평가돼 있었다"는 가설을 처음으로 실제 데이터가 지지하는 방향이나 n=2라 확정 불가.
- 발견한 패턴(참고, 결론은 보류): 펀딩비 크기·Trend30Pct 구간분석·2차원교차·B전략유리조건 분석은 이번 윈도우의 신규 레코드가 트레이드2건+스킵5건(GWEIUSDT 01·02·03시, VANAUSDT·TLMUSDT 04시)으로 총 n=7뿐이라 구간을 나눌 만한 표본이 안 됨 — 전체 평균만 참고하면 |FundingRatePct| vs C/Sim 평균+0.204%/승률86%(6승1무)로 방향 자체는 나쁘지 않으나 표본이 너무 작아 결론 없음, 다음 사이클에 축적 필요. `.ui` 로그에 "직전추세 필터 - C진입 스킵"(PreTrendSkip)이 여러 차례 찍히는데 `skipped_results.jsonl`엔 07-14 20:00 UTC 이후 PreTrendSkip 기록이 없음(그 이후 전부 MomentumNotConfirmed만 기록) — 로깅 누락 가능성, 코드영역이라 참고 기록만.
- 파라미터 변경: 없음 — n=2로 최소기준(5건)에 크게 미달, 게다가 코드패치가 이 윈도우 도중 배포돼 판단 근거 자체가 부족. "확신 없으면 안 바꾼다" 원칙 적용, `MaxTickPriceRatioPct` 포함 전 파라미터 유지(방금 배포된 필드라 성과 데이터 자체가 아직 없음).
- 롤백: 해당없음 — 표본부족(2건). 유효표본(≥5건) 사이클은 여전히 12·14회차 둘뿐.
- 다음 사이클이 알아야 할 것:
  - **다음 사이클은 사실상 처음으로 "완전히 패치 이후"인 클린 윈도우가 될 가능성이 높음** — B_Est/GWEIUSDT필터 관련 분석 신뢰도가 이번보다 크게 개선될 것, A/B/C 비교와 B전략분석을 우선적으로 재확인할 것.
  - GWEIUSDT가 계속 tick/price 필터에 걸려 후보에서 빠지는지(0.21%대 유지) 확인 - 재등장하면 필터 미적용/설정 문제 등 이상신호로 볼 것.
  - "A_Est가 C상회" 3연속(13,14,16회차)이 다음 사이클(표본 5건+)에서도 재현되는지 최우선 재확인 — 이번(17회차)은 n=2라 판단 불가.
  - 펀딩비크기/Trend30Pct/2차원교차/B전략분석 모두 이번엔 표본이 너무 작아(n=7 이하) 본격 수행 못함 — 다음 사이클부터 GWEIUSDT 없는 클린 데이터가 쌓이면 제대로 수행할 것.
  - PreTrendSkip 로깅 누락 의심 — 사람에게 참고 보고, 코드수정은 이 작업 범위 밖.
  - ⚠️전략재검토필요: 유지(재확인 보류 상태).
  - 이번 사이클에서 CLAUDE.md가 20KB 임계값을 다시 넘어 13~14회차 상세 로그를 `CLAUDE_MD_ARCHIVE_20260715B.md`로 이동, "과거 요약"을 5~14회차로 확장함.
  - 파일 정리 메모(9회차부터 이월, 우선순위 낮음): `automation_summary - 복사본.log` → `AUTOMATION_SUMMARY_ARCHIVE_20260713.log` 이름 변경 필요, 유니코드 파일명 문제로 도구 자동처리 안 됨 — 사람 수동 처리 필요.

## 자동튜닝 사이클 로그 [2026-07-15T07:03:28Z]
- 표본: 0건 (기간: 17회차 컷오프 2026-07-15T04:00:16Z 이후 ~ 2026-07-15T07:03:28Z UTC, 약 3시간). `trade_results.jsonl`(69줄)·`skipped_results.jsonl`(12줄) 둘 다 마지막 레코드가 04:00:15Z에 멈춰있어 이번 윈도우 신규 라인 자체가 없음. A/B/C·Trend30Pct·펀딩비크기·B전략 4개 분석 모두 표본 0으로 수행 불가.
- A/B/C 비교: 해당없음 (표본 0건).
- 발견한 패턴: `.ui` 로그(04:59/05:59/06:59 UTC 스캔 3회, `Log/20260715/CoinSvr_20260715.ui`) 확인 결과 거래중단/청산실패/수동확인/중복/DailyLossLimit 키워드 0건, ListenKey keepalive·서버시간 동기화 등 정상 동작 로그만 확인됨 — 트레이드 0건은 인프라 이슈가 아니라 "후보 자체가 없거나 PreTrendSkip으로 걸러짐"이 원인. 04:59 UTC 후보 1개(1000XECUSDT, 펀딩비-0.261%)가 직전30s추세-0.2172%(쏠림방향 급행)로 PreTrendSkip, 05:59 UTC도 동일 패턴(추세-0.3255%), 06:59 UTC는 "수수료 하한(0.08%)도 못 넘는 코인 제외 후 후보 0개"로 유효 후보 자체가 없어 스캔이 트레이드 없이 종료. GWEIUSDT는 tick/price 필터에 3회 연속 걸려 제외(0.214%→0.216%→0.220%로 완만히 상승, 여전히 정상군 대비 10배) — 17회차부터 필터가 계속 정상 작동 중임을 재확인. 또한 **"PreTrendSkip 로깅 누락 의심"(17회차 플래그)의 원인이 이번에 규명됨**: `.ui` 로그에 "🧪 스킵기록(PreTrendSkip): 정산 트리거 미수신 - 기록 안 함"이 명시적으로 찍혀 있어, PreTrendSkip이 정산 트리거를 3초 내에 못 받은 케이스에서는 `skipped_results.jsonl`에 의도적으로 기록되지 않는 기존 동작임을 확인(코드 버그 아님) — 이 플래그는 해제한다.
- 파라미터 변경: 없음 — 표본 0건, "확신 없으면 안 바꾼다" 원칙이 자명하게 적용됨.
- 롤백: 해당없음 — 비교 대상 자체가 없음(유효표본≥5건 사이클 여전히 12·14회차 둘뿐).
- 다음 사이클이 알아야 할 것:
  - **"A_Est가 C상회" 3/3(13,14,16회차) 재현 여부 재확인이 여전히 최우선 과제** — 이번 사이클은 표본 0건이라 다시 이월. 다음 사이클에 표본 5건 이상 쌓이면 반드시 재확인.
  - 이번 3시간 동안 후보 자체가 희소(펀딩비/수수료 하한 미달)했던 것이 일시적인지 구조적인지 다음 사이클에서 계속 관찰. tick/price 필터(GWEIUSDT 제외)는 정상 작동 지속 확인, 재등장하면 이상신호.
  - PreTrendSkip 로깅 "누락"은 버그가 아니라 "정산 트리거 미수신 시 기록 안 함"이라는 기존 설계임을 확인 — 관련 플래그 해제, 이후 재발 시엔 별개 이슈로 취급할 것.
  - 파일 정리: `automation_summary - 복사본.log`를 `AUTOMATION_SUMMARY_ARCHIVE_20260713.log`로 이름 변경하는 작업이 9회차부터 이월되어 있었으나 이번 사이클에서 성공적으로 완료됨(유니코드 파일명 문제가 이번엔 재현되지 않음) — 다음 사이클은 이 메모를 더 이상 이월할 필요 없음.
  - 이번 사이클에서 CLAUDE.md가 20KB 임계값을 다시 넘어 "사람과의 대화 세션 요약"+15~16회차 상세 로그를 `CLAUDE_MD_ARCHIVE_20260715C.md`로 이동, "과거 요약"을 5~16회차+패치세션으로 확장. `automation_summary.log`도 함께 20KB를 넘어 1~10회차를 `AUTOMATION_SUMMARY_ARCHIVE_20260715.log`(9~10회차+누적패턴트래커, 1~8회차는 기존 `AUTOMATION_SUMMARY_ARCHIVE_20260713.log`)로 압축, 아울러 17회차 항목이 automation_summary.log에 append 누락되어 있던 것을 발견해 이번 사이클에서 소급 보완함(15회차 CLAUDE.md 누락, 16회차 automation_summary.log 누락에 이어 3번째 상호누락 — 두 파일 append를 매 사이클 종료 직전 서로 grep으로 교차확인하는 습관화 필요).
